# Golden Master Snapshots

These XML files are generated from the test fixtures and serve as baseline references
for regression comparison when the serializer is refactored or replaced by auto-generation.

To regenerate: run the golden master tests which write the files on first run.
